/**
 * ContainerRegistryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ImportPipelineSourceProperties model module.
 * @module model/ImportPipelineSourceProperties
 * @version 2019-06-01-preview
 */
class ImportPipelineSourceProperties {
    /**
     * Constructs a new <code>ImportPipelineSourceProperties</code>.
     * The properties of the import pipeline source.
     * @alias module:model/ImportPipelineSourceProperties
     * @param keyVaultUri {String} They key vault secret uri to obtain the source storage SAS token.
     */
    constructor(keyVaultUri) { 
        
        ImportPipelineSourceProperties.initialize(this, keyVaultUri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, keyVaultUri) { 
        obj['keyVaultUri'] = keyVaultUri;
    }

    /**
     * Constructs a <code>ImportPipelineSourceProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImportPipelineSourceProperties} obj Optional instance to populate.
     * @return {module:model/ImportPipelineSourceProperties} The populated <code>ImportPipelineSourceProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImportPipelineSourceProperties();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
            if (data.hasOwnProperty('keyVaultUri')) {
                obj['keyVaultUri'] = ApiClient.convertToType(data['keyVaultUri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImportPipelineSourceProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImportPipelineSourceProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ImportPipelineSourceProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }
        // ensure the json data is a string
        if (data['keyVaultUri'] && !(typeof data['keyVaultUri'] === 'string' || data['keyVaultUri'] instanceof String)) {
            throw new Error("Expected the field `keyVaultUri` to be a primitive type in the JSON string but got " + data['keyVaultUri']);
        }

        return true;
    }


}

ImportPipelineSourceProperties.RequiredProperties = ["keyVaultUri"];

/**
 * The type of source for the import pipeline.
 * @member {module:model/ImportPipelineSourceProperties.TypeEnum} type
 * @default 'AzureStorageBlobContainer'
 */
ImportPipelineSourceProperties.prototype['type'] = 'AzureStorageBlobContainer';

/**
 * The source uri of the import pipeline.  When 'AzureStorageBlob': \"https://accountName.blob.core.windows.net/containerName/blobName\"  When 'AzureStorageBlobContainer': \"https://accountName.blob.core.windows.net/containerName\"
 * @member {String} uri
 */
ImportPipelineSourceProperties.prototype['uri'] = undefined;

/**
 * They key vault secret uri to obtain the source storage SAS token.
 * @member {String} keyVaultUri
 */
ImportPipelineSourceProperties.prototype['keyVaultUri'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ImportPipelineSourceProperties['TypeEnum'] = {

    /**
     * value: "AzureStorageBlobContainer"
     * @const
     */
    "AzureStorageBlobContainer": "AzureStorageBlobContainer"
};



export default ImportPipelineSourceProperties;

