/**
 * ContainerRegistryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SetValue model module.
 * @module model/SetValue
 * @version 2019-06-01-preview
 */
class SetValue {
    /**
     * Constructs a new <code>SetValue</code>.
     * The properties of a overridable value that can be passed to a task template.
     * @alias module:model/SetValue
     * @param name {String} The name of the overridable value.
     * @param value {String} The overridable value.
     */
    constructor(name, value) { 
        
        SetValue.initialize(this, name, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, value) { 
        obj['name'] = name;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>SetValue</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SetValue} obj Optional instance to populate.
     * @return {module:model/SetValue} The populated <code>SetValue</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SetValue();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
            if (data.hasOwnProperty('isSecret')) {
                obj['isSecret'] = ApiClient.convertToType(data['isSecret'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SetValue</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SetValue</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SetValue.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

SetValue.RequiredProperties = ["name", "value"];

/**
 * The name of the overridable value.
 * @member {String} name
 */
SetValue.prototype['name'] = undefined;

/**
 * The overridable value.
 * @member {String} value
 */
SetValue.prototype['value'] = undefined;

/**
 * Flag to indicate whether the value represents a secret or not.
 * @member {Boolean} isSecret
 * @default false
 */
SetValue.prototype['isSecret'] = false;






export default SetValue;

