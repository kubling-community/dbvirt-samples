/**
 * ContainerRegistryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExportPipelineTargetProperties model module.
 * @module model/ExportPipelineTargetProperties
 * @version 2019-06-01-preview
 */
class ExportPipelineTargetProperties {
    /**
     * Constructs a new <code>ExportPipelineTargetProperties</code>.
     * The properties of the export pipeline target.
     * @alias module:model/ExportPipelineTargetProperties
     * @param keyVaultUri {String} They key vault secret uri to obtain the target storage SAS token.
     */
    constructor(keyVaultUri) { 
        
        ExportPipelineTargetProperties.initialize(this, keyVaultUri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, keyVaultUri) { 
        obj['keyVaultUri'] = keyVaultUri;
    }

    /**
     * Constructs a <code>ExportPipelineTargetProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExportPipelineTargetProperties} obj Optional instance to populate.
     * @return {module:model/ExportPipelineTargetProperties} The populated <code>ExportPipelineTargetProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExportPipelineTargetProperties();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
            if (data.hasOwnProperty('keyVaultUri')) {
                obj['keyVaultUri'] = ApiClient.convertToType(data['keyVaultUri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExportPipelineTargetProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExportPipelineTargetProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExportPipelineTargetProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }
        // ensure the json data is a string
        if (data['keyVaultUri'] && !(typeof data['keyVaultUri'] === 'string' || data['keyVaultUri'] instanceof String)) {
            throw new Error("Expected the field `keyVaultUri` to be a primitive type in the JSON string but got " + data['keyVaultUri']);
        }

        return true;
    }


}

ExportPipelineTargetProperties.RequiredProperties = ["keyVaultUri"];

/**
 * The type of target for the export pipeline.
 * @member {String} type
 */
ExportPipelineTargetProperties.prototype['type'] = undefined;

/**
 * The target uri of the export pipeline.  When 'AzureStorageBlob': \"https://accountName.blob.core.windows.net/containerName/blobName\"  When 'AzureStorageBlobContainer':  \"https://accountName.blob.core.windows.net/containerName\"
 * @member {String} uri
 */
ExportPipelineTargetProperties.prototype['uri'] = undefined;

/**
 * They key vault secret uri to obtain the target storage SAS token.
 * @member {String} keyVaultUri
 */
ExportPipelineTargetProperties.prototype['keyVaultUri'] = undefined;






export default ExportPipelineTargetProperties;

