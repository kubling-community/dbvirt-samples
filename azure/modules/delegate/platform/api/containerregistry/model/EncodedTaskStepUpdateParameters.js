/**
 * ContainerRegistryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SetValue from './SetValue';
import TaskStepUpdateParameters from './TaskStepUpdateParameters';

/**
 * The EncodedTaskStepUpdateParameters model module.
 * @module model/EncodedTaskStepUpdateParameters
 * @version 2019-06-01-preview
 */
class EncodedTaskStepUpdateParameters {
    /**
     * Constructs a new <code>EncodedTaskStepUpdateParameters</code>.
     * The properties for updating encoded task step.
     * @alias module:model/EncodedTaskStepUpdateParameters
     * @extends module:model/TaskStepUpdateParameters
     * @implements module:model/TaskStepUpdateParameters
     * @param type {module:model/EncodedTaskStepUpdateParameters.TypeEnum} The type of the step.
     */
    constructor(type) { 
        TaskStepUpdateParameters.initialize(this, type);
        EncodedTaskStepUpdateParameters.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
    }

    /**
     * Constructs a <code>EncodedTaskStepUpdateParameters</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EncodedTaskStepUpdateParameters} obj Optional instance to populate.
     * @return {module:model/EncodedTaskStepUpdateParameters} The populated <code>EncodedTaskStepUpdateParameters</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EncodedTaskStepUpdateParameters();
            TaskStepUpdateParameters.constructFromObject(data, obj);
            TaskStepUpdateParameters.constructFromObject(data, obj);

            if (data.hasOwnProperty('encodedTaskContent')) {
                obj['encodedTaskContent'] = ApiClient.convertToType(data['encodedTaskContent'], 'String');
            }
            if (data.hasOwnProperty('encodedValuesContent')) {
                obj['encodedValuesContent'] = ApiClient.convertToType(data['encodedValuesContent'], 'String');
            }
            if (data.hasOwnProperty('values')) {
                obj['values'] = ApiClient.convertToType(data['values'], [SetValue]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EncodedTaskStepUpdateParameters</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EncodedTaskStepUpdateParameters</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EncodedTaskStepUpdateParameters.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['encodedTaskContent'] && !(typeof data['encodedTaskContent'] === 'string' || data['encodedTaskContent'] instanceof String)) {
            throw new Error("Expected the field `encodedTaskContent` to be a primitive type in the JSON string but got " + data['encodedTaskContent']);
        }
        // ensure the json data is a string
        if (data['encodedValuesContent'] && !(typeof data['encodedValuesContent'] === 'string' || data['encodedValuesContent'] instanceof String)) {
            throw new Error("Expected the field `encodedValuesContent` to be a primitive type in the JSON string but got " + data['encodedValuesContent']);
        }
        if (data['values']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['values'])) {
                throw new Error("Expected the field `values` to be an array in the JSON data but got " + data['values']);
            }
            // validate the optional field `values` (array)
            for (const item of data['values']) {
                SetValue.validateJSON(item);
            };
        }

        return true;
    }


}

EncodedTaskStepUpdateParameters.RequiredProperties = ["type"];

/**
 * Base64 encoded value of the template/definition file content.
 * @member {String} encodedTaskContent
 */
EncodedTaskStepUpdateParameters.prototype['encodedTaskContent'] = undefined;

/**
 * Base64 encoded value of the parameters/values file content.
 * @member {String} encodedValuesContent
 */
EncodedTaskStepUpdateParameters.prototype['encodedValuesContent'] = undefined;

/**
 * The collection of overridable values that can be passed when running a task.
 * @member {Array.<module:model/SetValue>} values
 */
EncodedTaskStepUpdateParameters.prototype['values'] = undefined;


// Implement TaskStepUpdateParameters interface:
/**
 * The type of the step.
 * @member {module:model/TaskStepUpdateParameters.TypeEnum} type
 */
TaskStepUpdateParameters.prototype['type'] = undefined;
/**
 * The URL(absolute or relative) of the source context for the task step.
 * @member {String} contextPath
 */
TaskStepUpdateParameters.prototype['contextPath'] = undefined;
/**
 * The token (git PAT or SAS token of storage account blob) associated with the context for a step.
 * @member {String} contextAccessToken
 */
TaskStepUpdateParameters.prototype['contextAccessToken'] = undefined;




export default EncodedTaskStepUpdateParameters;

