/**
 * ContainerRegistryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BaseImageDependency from './BaseImageDependency';

/**
 * The TaskStepProperties model module.
 * @module model/TaskStepProperties
 * @version 2019-06-01-preview
 */
class TaskStepProperties {
    /**
     * Constructs a new <code>TaskStepProperties</code>.
     * Base properties for any task step.
     * @alias module:model/TaskStepProperties
     * @param type {module:model/TaskStepProperties.TypeEnum} The type of the step.
     */
    constructor(type) { 
        
        TaskStepProperties.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['type'] = type;
    }

    /**
     * Constructs a <code>TaskStepProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TaskStepProperties} obj Optional instance to populate.
     * @return {module:model/TaskStepProperties} The populated <code>TaskStepProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TaskStepProperties();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('baseImageDependencies')) {
                obj['baseImageDependencies'] = ApiClient.convertToType(data['baseImageDependencies'], [BaseImageDependency]);
            }
            if (data.hasOwnProperty('contextPath')) {
                obj['contextPath'] = ApiClient.convertToType(data['contextPath'], 'String');
            }
            if (data.hasOwnProperty('contextAccessToken')) {
                obj['contextAccessToken'] = ApiClient.convertToType(data['contextAccessToken'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TaskStepProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TaskStepProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TaskStepProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        if (data['baseImageDependencies']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['baseImageDependencies'])) {
                throw new Error("Expected the field `baseImageDependencies` to be an array in the JSON data but got " + data['baseImageDependencies']);
            }
            // validate the optional field `baseImageDependencies` (array)
            for (const item of data['baseImageDependencies']) {
                BaseImageDependency.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['contextPath'] && !(typeof data['contextPath'] === 'string' || data['contextPath'] instanceof String)) {
            throw new Error("Expected the field `contextPath` to be a primitive type in the JSON string but got " + data['contextPath']);
        }
        // ensure the json data is a string
        if (data['contextAccessToken'] && !(typeof data['contextAccessToken'] === 'string' || data['contextAccessToken'] instanceof String)) {
            throw new Error("Expected the field `contextAccessToken` to be a primitive type in the JSON string but got " + data['contextAccessToken']);
        }

        return true;
    }


}

TaskStepProperties.RequiredProperties = ["type"];

/**
 * The type of the step.
 * @member {module:model/TaskStepProperties.TypeEnum} type
 */
TaskStepProperties.prototype['type'] = undefined;

/**
 * List of base image dependencies for a step.
 * @member {Array.<module:model/BaseImageDependency>} baseImageDependencies
 */
TaskStepProperties.prototype['baseImageDependencies'] = undefined;

/**
 * The URL(absolute or relative) of the source context for the task step.
 * @member {String} contextPath
 */
TaskStepProperties.prototype['contextPath'] = undefined;

/**
 * The token (git PAT or SAS token of storage account blob) associated with the context for a step.
 * @member {String} contextAccessToken
 */
TaskStepProperties.prototype['contextAccessToken'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
TaskStepProperties['TypeEnum'] = {

    /**
     * value: "Docker"
     * @const
     */
    "Docker": "Docker",

    /**
     * value: "FileTask"
     * @const
     */
    "FileTask": "FileTask",

    /**
     * value: "EncodedTask"
     * @const
     */
    "EncodedTask": "EncodedTask"
};



export default TaskStepProperties;

