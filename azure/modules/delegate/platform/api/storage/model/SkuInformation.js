/**
 * StorageManagementClient
 * The Azure Storage Management API.
 *
 * The version of the OpenAPI document: 2023-01-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from "../../BaseApiClient";
import Restriction from './Restriction';
import SKUCapability from './SKUCapability';
import SkuName from './SkuName';
import Tier from './Tier';

/**
 * The SkuInformation model module.
 * @module model/SkuInformation
 * @version 2023-01-01
 */
class SkuInformation {
    /**
     * Constructs a new <code>SkuInformation</code>.
     * Storage SKU and its properties
     * @alias module:model/SkuInformation
     * @param name {module:model/SkuName} 
     */
    constructor(name) { 
        
        SkuInformation.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>SkuInformation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SkuInformation} obj Optional instance to populate.
     * @return {module:model/SkuInformation} The populated <code>SkuInformation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SkuInformation();

            if (data.hasOwnProperty('name')) {
                obj['name'] = SkuName.constructFromObject(data['name']);
            }
            if (data.hasOwnProperty('tier')) {
                obj['tier'] = Tier.constructFromObject(data['tier']);
            }
            if (data.hasOwnProperty('resourceType')) {
                obj['resourceType'] = ApiClient.convertToType(data['resourceType'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('locations')) {
                obj['locations'] = ApiClient.convertToType(data['locations'], ['String']);
            }
            if (data.hasOwnProperty('capabilities')) {
                obj['capabilities'] = ApiClient.convertToType(data['capabilities'], [SKUCapability]);
            }
            if (data.hasOwnProperty('restrictions')) {
                obj['restrictions'] = ApiClient.convertToType(data['restrictions'], [Restriction]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SkuInformation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SkuInformation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SkuInformation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['resourceType'] && !(typeof data['resourceType'] === 'string' || data['resourceType'] instanceof String)) {
            throw new Error("Expected the field `resourceType` to be a primitive type in the JSON string but got " + data['resourceType']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['locations'])) {
            throw new Error("Expected the field `locations` to be an array in the JSON data but got " + data['locations']);
        }
        if (data['capabilities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['capabilities'])) {
                throw new Error("Expected the field `capabilities` to be an array in the JSON data but got " + data['capabilities']);
            }
            // validate the optional field `capabilities` (array)
            for (const item of data['capabilities']) {
                SKUCapability.validateJSON(item);
            };
        }
        if (data['restrictions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['restrictions'])) {
                throw new Error("Expected the field `restrictions` to be an array in the JSON data but got " + data['restrictions']);
            }
            // validate the optional field `restrictions` (array)
            for (const item of data['restrictions']) {
                Restriction.validateJSON(item);
            };
        }

        return true;
    }


}

SkuInformation.RequiredProperties = ["name"];

/**
 * @member {module:model/SkuName} name
 */
SkuInformation.prototype['name'] = undefined;

/**
 * @member {module:model/Tier} tier
 */
SkuInformation.prototype['tier'] = undefined;

/**
 * The type of the resource, usually it is 'storageAccounts'.
 * @member {String} resourceType
 */
SkuInformation.prototype['resourceType'] = undefined;

/**
 * Indicates the type of storage account.
 * @member {module:model/SkuInformation.KindEnum} kind
 */
SkuInformation.prototype['kind'] = undefined;

/**
 * The set of locations that the SKU is available. This will be supported and registered Azure Geo Regions (e.g. West US, East US, Southeast Asia, etc.).
 * @member {Array.<String>} locations
 */
SkuInformation.prototype['locations'] = undefined;

/**
 * The capability information in the specified SKU, including file encryption, network ACLs, change notification, etc.
 * @member {Array.<module:model/SKUCapability>} capabilities
 */
SkuInformation.prototype['capabilities'] = undefined;

/**
 * The restrictions because of which SKU cannot be used. This is empty if there are no restrictions.
 * @member {Array.<module:model/Restriction>} restrictions
 */
SkuInformation.prototype['restrictions'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
SkuInformation['KindEnum'] = {

    /**
     * value: "Storage"
     * @const
     */
    "Storage": "Storage",

    /**
     * value: "StorageV2"
     * @const
     */
    "StorageV2": "StorageV2",

    /**
     * value: "BlobStorage"
     * @const
     */
    "BlobStorage": "BlobStorage",

    /**
     * value: "FileStorage"
     * @const
     */
    "FileStorage": "FileStorage",

    /**
     * value: "BlockBlobStorage"
     * @const
     */
    "BlockBlobStorage": "BlockBlobStorage"
};



export default SkuInformation;

